require 'rails_helper'

RSpec.describe "Agenciaaereas", :type => :request do
  describe "GET /agenciaaereas" do
    it "works! (now write some real specs)" do
      get agenciaaereas_path
      expect(response.status).to be(200)
    end
  end
end
